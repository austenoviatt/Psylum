//Test program for menu and ascii art

#include <fstream>
#include <sstream>
#include <string>

using namespace std;

int main(){

   string str;
   fstream logo;
   logo.open("logoAscii.txt");
   ifstream logo("logoAscii.txt");
   while (!logo.eof()) {
      logo >> str;
      cout << str << ebdkl
   

      //inData.open("logoAscii.txt");
	 return 0;
}

