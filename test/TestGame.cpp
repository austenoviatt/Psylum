#include "gtest/gtest.h"
#include "Game.h"
#include <vector>
#include <string>

using namespace std;

//TEST(Game, processCommand) {
  //Game g;
  //string str = "INsPect TO AnenrE";
  //vector<string>strr = g.processCommand(str);
  //EXPECT_EQ("udnskeiin", g.processCommand(str));
  //EXPECT_EQ("anenre", g.processCommand(str));
  //string result = g.processCommand(str);
  //EXPECT_EQ("anenre", result);
//}

